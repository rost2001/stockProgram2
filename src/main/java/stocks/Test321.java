package stocks;

public class Test321 {
    /* Acknowledgements:
     * Selection, sequencing, probiblities, time, 
     * 
     * 
     */
    
    
    public static void main(String[] args) {
	/* A Sequencing Relative Point System
	 * ----------------------------------
	 * Through sequencing and probabilities, while handling of time.
	 * Find the sequences that has favorable probabilities, and whatever time needs to be handled.
	 * Have that relative to all of the selected stocks in a list, could be a stock screener,
	 * a watchlist, or 3 stocks, or any number, a selection.
	 * Stocks in that list gets points for how they compare to eachother.
	 * 
	 * 
	 */
	
	
	
	/* A selector into list, viewed on interval like 30 sec, on a secondscreen
	 * ------------------------------------------------------------------------
	 * List taken from stockscreener or watchlist. And updated regularly. Prehaps a way to switch delimiters.
	 * Second screen. Different window.
	 * Interval short enough to not take large part of day, but long enough to get a look at it,
	 * alternative one can remove items of list as considered poor and not to be included to view again,
	 * aswell as a way to reset the list to view all lists again.
	 * Have a button to paus, have one for adding it to another window on another screen.
	 * 
	 * 
	 * ---------------------------------------------
	 * Same display version:
	 * --------
	 * Buttons_to reset, to paus/resume, to add to watchlist, to remove stock from spotlight, to swtich sorted category.
	 * 10 tabs of the most top stocks of the sorted category with delimiters.
	 * Order not important, ||||(-no-) saving it is. Add remove from table method.
	 * List of the 10 tabs. a List of top stocks is gotten periodically and checked with it.
	 * _if a symbol do not match with any then find which tab holds a symbol not matching any of the most recent list, and replace.
	 * List of removed ones, if newly symbol match here then just skip and go next.
	 * 
	 * need_ selenium multiple tabs and changes in background to those tabs
	 * _ method to add to watchlist in CBTradingview
	 * 
	 * 
	 * 
	 * 
	 * 
	 */

    }

}
